// Copyright (c) FIRST and other WPILib contributors.
// Open Source Software; you can modify and/or share it under the terms of
// the WPILib BSD license file in the root directory of this project.

package frc.robot.commands;

import edu.wpi.first.wpilibj2.command.CommandBase;
import frc.robot.RobotContainer;


public class MecDrive extends CommandBase {

  public static final double speedLimit = 0.4;
  /** Creates a new MecDrive. */
  public MecDrive() {
    // Use addRequirements() here to declare subsystem dependencies.
    addRequirements(RobotContainer.driveTrain);
  }

  // Called when the command is initially scheduled.
  @Override
  public void initialize() {}

  // Called every time the scheduler runs while the command is scheduled.
  @Override
  public void execute() {
    
    double leftStickY = 0.5 * RobotContainer.driverController_1.getY();
    double rightStickY = 0.5 * RobotContainer.driverController_2.getY();
    double leftStickX = 0.5 * RobotContainer.driverController_1.getX();
    double rightStickX = 0.5 * RobotContainer.driverController_2.getX();
   
    // RobotContainer.driveTrain.setLeftMotors(leftStickY);
    // RobotContainer.driveTrain.setRightMotors(-rightStickY);
    //RobotContainer.driveTrain.mecanumDrive(leftStickX);
    //RobotContainer.driveTrain.mecanumDrive(-rightStickX);
    RobotContainer.driveTrain.mecanumDrive(leftStickY, -leftStickX, -rightStickX);
  }

  // Called once the command ends or is interrupted.
  @Override
  public void end(boolean interrupted) {
    RobotContainer.driveTrain.mecanumDrive(0,0,0);
  }

  // Returns true when the command should end.
  @Override
  public boolean isFinished() {
    return false;
  }
}

